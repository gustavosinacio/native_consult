import styled from 'styled-components/native';
import Icon from 'react-native-vector-icons/MaterialCommunityIcons';

import { mainTextColor } from '../../assets/colors';

export const StyledFormIcon = styled(Icon)`
  color: ${mainTextColor};
  margin: 10px;
`;
